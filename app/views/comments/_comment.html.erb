<%= form_with model: @comment do |form| %>
    Content: <%= form.text_field :content %>
    Post: <%= form.collection_select :post_id, Post.all.order(:title), :id, :title %>
    
    <%= form.submit %>
<% end %>